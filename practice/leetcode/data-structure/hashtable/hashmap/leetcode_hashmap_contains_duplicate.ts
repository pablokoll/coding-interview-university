function containsNearbyDuplicate(nums: number[], k: number): boolean {  
    const mapContainers = new Map<number, number>();

    for(const num of nums){
        // mapContainers.set(num, )
    }

    return false;
}
